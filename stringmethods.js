// string methods 
// strings are always immutable i.e they are never changed . meaning 
// if we try to change a string it will create a new string and return it
// so we can say that strings are immutable
// but we can change the string by using string methods
// string methods are used to change the string
// string methods are used to find the string
// string methods are used to replace the string
// string methods are used to split the string
// string methods are used to join the string
// string methods are used to convert the string to uppercase and lowercase
// string methods are used to find the length of the string
// string methods are used to find the index of the string
// string methods are used to find the last index of the string
// string methods are used to find the character at the index of the string
// string methods are used to find the last character at the index of the string

let str = "jeewan";

//by default the new string variable is generated and stored the value or the string 
// and the old string variable is not changed , so to access the new value we do this 
newstring = str.toUpperCase();

//if we want to change the original value to upper case 
str = str.toUpperCase();

console.log(str);
console.log(newstring); 
console.log(newstring);
console.log(str.length);
console.log(str.toUpperCase());
console.log(str.toLowerCase());
console.log(str.charAt(0));
console.log(str.charAt(1));
console.log(str.charAt(2));

